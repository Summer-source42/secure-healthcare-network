# Configuration Files

This directory contains clean, portfolio-ready configuration templates.

## Important Notes

- Replace placeholder values such as `<STRONG_SECRET>`, `<ADMIN_PC_IP>`, and `<INTERFACE>` before using.
- Do not publish real passwords or production IPs.
- Packet Tracer support may vary by device model.
- Add final running configurations from your completed Packet Tracer lab after sanitizing passwords.

## Suggested Final Files to Add

```text
switches/mls1-running-config.txt
switches/mls2-running-config.txt
switches/access-switch-pharmacy-lab.txt
switches/access-switch-reception-guest.txt
routers/edge-router-running-config.txt
routers/voip-router-running-config.txt
firewall/asa-running-config.txt
wireless/wlc-settings-screenshots.md
```
